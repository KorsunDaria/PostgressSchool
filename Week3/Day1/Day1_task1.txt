Вопрос 1. Создайте таблицу и рассчитайте точное количество строк, 
необходимое для того, чтобы полностью заполнить все 4069 слотов на первой листовой 
FSM-странице (учтите размер кортежа integer и выравнивание). 
Вставьте это количество строк, сделайте VACUUM, а затем добавьте еще одну строку, 
чтобы инициировать переход на вторую листовую FSM-страницу. 
С помощью pageinspect посмотрите на содержимое нелистовой FSM-страницы. 
Почему значение fp_next_slot на нелистовой странице в этот момент оказывается равным 1, 
а не 2, как можно было бы ожидать по аналогии с листовыми страницами? 
Объясните фундаментальную разницу в том, как PostgreSQL кэширует номера обычных 
страниц и номера FSM-страниц.

~/pginstall/bin/psql -d postgres

CREATE EXTENSION IF NOT EXISTS pageinspect;
CREATE TABLE fsm_q1 (id integer);

226 × 4069 = 919 594 строки

INSERT INTO fsm_q1 SELECT generate_series(1, 919594);
VACUUM fsm_q1;

SELECT pg_relation_size('fsm_q1', 'fsm');  

SELECT * FROM fsm_page_contents(get_raw_page('fsm_q1', 'fsm', 0));

Вопрос 2. Создайте таблицу и вставьте в неё ровно 226 строк 
(каждая занимает 36 байт с учетом указателя), чтобы на первой странице основного
слоя осталось ровно 32 байта свободного пространства. С помощью page_header 
зафиксируйте точное значение upper - lower. Затем с помощью fsm_page_contents 
посмотрите категорию этой страницы в FSM. Почему категория в FSM оказывается на шаг 
ниже, чем должно быть по "сырому" значению свободного места? Докажите, откуда берется
 эта разница, и объясните, какой скрытый механизм PostgreSQL заставляет FSM заранее 
 "вычитать" эти байты при расчете категории.